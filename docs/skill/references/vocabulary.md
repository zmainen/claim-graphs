# The claim vocabulary

{{generated-note}}

This is the contract file the pipeline sends to every model call, reproduced here in full and
byte-identical. An agent working from this page and a model call made by `claim-graphs` are
given the same definitions and the same examples — that is the point of rendering it rather
than restating it.

{{contract-vocabulary}}
